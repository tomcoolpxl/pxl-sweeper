export const V2_CONFIG = {
    // Layout & Scaling
    LAYOUT: {
        HUD_HEIGHT: 100,
        BASE_PADDING: 2,
        MAX_TILE_SIZE: 32,
        MARGIN_PERCENT: 0.95
    },
    // Timers & Delays
    TIMERS: {
        LONG_PRESS_MS: 300,
        THROTTLE_REVEAL_SFX_MS: 50,
        GAMEOVER_FADE_MS: 500
    },
    // Haptics
    HAPTICS: {
        WIN: [100, 50, 100],
        LOSS: 200,
        FLAG: 50
    },
    // Accessibility
    A11Y: {
        HUD_ID: 'a11y-hud'
    }
};
